// Ask 页面专用 JavaScript 功能
document.addEventListener('DOMContentLoaded', function() {
    console.log('Ask page loaded');
    
    // Ask 页面的特定功能可以在这里添加
    // 例如：搜索功能、过滤功能等
    
    // 初始化 Ask 页面的交互功能
    initializeAskPage();
});

function initializeAskPage() {
    // 这里可以添加 Ask 页面的初始化逻辑
    // 例如：设置搜索框、绑定事件等
    console.log('Ask page initialized');
} 